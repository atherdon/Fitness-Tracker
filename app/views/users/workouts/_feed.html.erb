<div class="feed row row-centered">




		<%= render 'users/workouts/add_workout' %>

		<% if @user.workouts.any? %>

		<div id="workouts">
		<%= render partial: 'users/workouts/workout', collection: @workouts, as: :workout %>
		</div>

		<% end %>
		
		<div id="infinite-scrolling">
  		<%= will_paginate @workouts %>
		</div>


</div>