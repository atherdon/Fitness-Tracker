

$('.activity-feed').append('<%= j render partial: 'users/feed', collection: @activities, as: :activity %>');
<% if @activities.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @activities %>');
<% else %>
  $(window).off('scroll');
  //$('.pagination').remove();
<% end %>

