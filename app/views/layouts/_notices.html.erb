<div class="notices">

  	<% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %> alert-dismissable notices-bar navbar-nav navbar-fixed-top" id="flash"><%= message %></div>

  	<% end %>

</div>